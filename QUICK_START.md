# 🚀 Quick Start Guide

Get up and running in 3 simple steps!

## Step 1: Install Dependencies

```bash
pip install -r requirements.txt
```

## Step 2: Run the App

**Windows:**
```bash
run_streamlit.bat
```

**Linux/Mac:**
```bash
chmod +x run_streamlit.sh
./run_streamlit.sh
```

**Or directly:**
```bash
streamlit run streamlit_app.py
```

## Step 3: Use the App

1. Open your browser (usually opens automatically at `http://localhost:8501`)
2. Enter your API key (Gemini or OpenAI) in the sidebar
3. Upload HTML files
4. Configure settings
5. Click "Start Processing"
6. Download your processed files!

## 📝 Need Help?

- Check `README.md` for detailed documentation
- Check `PROJECT_STRUCTURE.md` for project organization
- Check `GITHUB_SETUP.md` for GitHub deployment

## 🎯 That's it!

Your app is ready to use. Happy rewriting! ✨
